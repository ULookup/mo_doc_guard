services:
  docs-agent-ops:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./runs:/app/runs
    restart: unless-stopped
